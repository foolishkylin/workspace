#include <stdio.h>
#include <stdlib.h>
typedef int ElementType;
typedef struct Node ListNode, *List; 
struct Node{
    ElementType D;
    struct Node *next; 
};

List List_Creat()
{
    ptr = (List) malloc (sizeof())
}